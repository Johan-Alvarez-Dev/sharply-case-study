openapi: 3.1.0
info:
  title: Sharply Public API Contract
  version: 1.0.0
paths:
  /api/public/reviews/due:
    get:
      summary: Return a privacy-safe due-review summary
      responses:
        '200': { description: Due-review summary }
